import { CountryInfoModelNamePipe } from './country-info-model-name.pipe';

describe('CountryInfoModelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CountryInfoModelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
